<% 
contacts = Contact.all 

%>

<h1> Contacts  </h1>

<% Contact.inspect %>

<ul>
  <% for contact in contacts %>
      <% company = Company.find_by({ "id" => contact["company_id"] }) %>

     <li> <%= contact["first_name"] %>
           <%= contact["last_name"] %>    - 
           <%= contact["email"] %>    - 
           <%= contact["phone_number"] %> - 
           <%= company["name"]%>
    </li>
  <% end %>
  

</ul>
